# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'Hound SwiftLint Demo' do
  use_frameworks!

  # A tool to enforce Swift style and conventions, loosely based on GitHub's Swift Style Guide.
  pod 'SwiftLint'

end
